import { StyleSheet } from 'react-native';

const estilo = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        padding: 8
    },
    curtiu: {
        color: 'red'
    },
    curtidas: {
        marginLeft: 8
    }
});

export default estilo;