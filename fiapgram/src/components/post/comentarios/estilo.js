import { StyleSheet } from 'react-native';

const estilo = StyleSheet.create({
    container: {
        flexDirection: 'row',
        paddingHorizontal: 8,
        paddingTop: 4
    },
    user: {
        fontWeight: 'bold',
        marginRight: 8
    }
});

export default estilo;