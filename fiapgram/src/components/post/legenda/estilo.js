import { StyleSheet } from 'react-native';

const estilo = StyleSheet.create({
    legenda: {
        paddingHorizontal: 8
    },
    usuario: {
        fontWeight: 'bold'
    }
});

export default estilo;
